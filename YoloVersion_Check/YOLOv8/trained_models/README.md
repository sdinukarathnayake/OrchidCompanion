# Trained Models

After Colab training finishes, download `orchid_yolov8_trained_output.zip` from the final notebook cell.

Extract the downloaded zip here and keep the important model file:

```text
weights/best.pt
```

Use `best.pt` for orchid stage detection/inference.
